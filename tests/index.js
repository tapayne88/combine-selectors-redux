import * as externalTestSelectors from "./__mocks__/testSelectors";
import combineReducers from "../src/index";

const localTestSelectors = () => ({
  selector1
});

describe("combineReducers", () => {
  it("should", () => {
    expect(true).toEqual(true);
  });
});
